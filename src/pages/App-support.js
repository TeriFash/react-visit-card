import React, { Component } from 'react';
// import ReactDOM from 'react-dom';

class AppSupport extends Component {
  constructor(props) {
    super(props);
    this.state = {
      
    };
  }

  render() {
    return <h1 className="Support-page">
        Support-page-text
      </h1>
  }
}

export default AppSupport;
